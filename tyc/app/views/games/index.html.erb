

<div class="container games_container">
	<div class="row">
			<div class="col-md-12">
			<% @games.each do |game| %>
				<%= link_to image_tag(game.game_img, :id=>'game_logo'), game %>
			<% end %>
		</div>
	</div>
</div>
